"""External-tool adapters.

Adapters are intentionally subprocess wrappers. Broadcast Kit validates and
normalizes contracts, then delegates implementation to the running repositories.

The ``notebooklm`` adapter is shipped as a sub-package
(``broadcast_kit.adapters.notebooklm``) — see ``adapters/notebooklm.md`` for the
boundary contract.
"""
